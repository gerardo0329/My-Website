let count = 0;

count = count + 1;
count;

let objectName = "water bottle";
let objectColor = "blue";
let objectAge = 2;
let isUseful = true;

let moods = ["sleepy", "nervous", "glowing", "dramatic"];

moods[0];
moods[1];
moods[2];
moods[3];

let tinyThing = {
  name: "Button Ghost",
  species: "interface creature",
  age: 3,
  isVisible: true
};

tinyThing.name;
tinyThing.species;
tinyThing.age;
tinyThing.isVisible;